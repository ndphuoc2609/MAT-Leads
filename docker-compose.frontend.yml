services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://192.168.1.26:8000}
        VITE_API_KEY: ${VITE_API_KEY:-}
    container_name: mat-leads-frontend
    ports:
      - "3000:3000"
    restart: unless-stopped
